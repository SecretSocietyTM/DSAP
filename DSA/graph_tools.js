import { ArrayList } from "./ArrayList.js";

const randInt = (min, max) => {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

// randomly generating valid graphs will be quite difficult
// a problem for another time

/* 
Need to prevent 
*/